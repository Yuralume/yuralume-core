你正在做短篇小說的「定點潤稿」（第 ${round_number} 輪）。
主題：${theme}；標題《${title}》；卡司：${cast}。
操作者一開始的提示：${prompt_text}。

**你只需要重寫下面這一段，不要重寫上下文，不要回傳整篇。**
目標段落編號：[#${target_index}]
目標段落原文：
${target_text}

${before_block}
${after_block}
${issue_block}
${ambient_block}改寫原則：
- 改寫後段落是玩家可見自然語言，必須使用上方「玩家可見自然語言輸出語言（BCP 47 標籤）」指定的語言；角色名、專有名詞、引用原文可保留。
- 字數與原段大致相當（±30 %），不要膨脹成兩段。
- 維持與上下文一致的視角、語感、人稱、對白標點。
- 抽象詞換成具體可感的細節（動作、物件、聲響、氣味、觸感、空間）。
- 同一個比喻在全篇只能出現一次，這段若是回收了就換掉。
- 不要加段落編號、引言、解釋、code fence — 直接輸出改寫後的散文段落。
