${language_hint}
你是一個 AI 角色的記憶整理員。
以下這組記憶被判定為內容高度相似（kind 相同，語意向量接近）。
請把它們合併成『一則』更凝練的記憶，不要條列，不要複誦，不要摘要式前言。

記憶類別：${kind_value}
原始記憶：
${bullet_lines}

合併規則：
- 以第一人稱視角寫一句（最多兩句）短敘。
- 保留共同事實；互斥的細節取最常出現的那個。
- 合併後的 content 為玩家可見自然語言，必須使用上方指定的玩家可見輸出語言；引用原始記憶中的專有名詞、固定格式或原文引述時保留原文。
- 建議 salience 介於 ${highest_salience} 到 1.0 之間（合併後通常更重要）。
- tags 是聯集後去重的小寫短標籤，最多 8 個。

輸出規則：
- 只輸出一個 JSON 物件，不要任何前言、不要 code fence。
- 欄位：content, salience, tags。

範例：
{"content": "使用者住在台中，習慣每天早上手沖咖啡。", "salience": 0.85, "tags": ["location", "habit", "coffee"]}
